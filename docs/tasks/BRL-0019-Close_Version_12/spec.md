# Scope

Close Version 12 only after its three milestones reflect accepted user feedback and current verification. Record the native evidence-capture retry acceptance represented by commit d1a7d28, the accepted projectile visual/hitbox/aim-trace correction, and the final Balance Lab defaults. Reconcile M03, the V12 roadmap, the documentation index, and V13's entry gate.

# Gates

- Canonical check and lint commands pass against the current worktree.
- The complete routed network and performance suites pass after final balance changes.
- M03 contains feedback dispositions, updated current defaults, exit evidence, and a learn-from-errors review.
- V12 status and every milestone are Complete.
- V13 entry gate changes from blocked on V12 to satisfied without changing V13 scope.
- Ticket mirrors are synchronized and unrelated user files are untouched.


## Closeout evidence — 2026-08-27

- V12 M03 native capture failure was corrected and accepted through commit `d1a7d28`; projectile geometry/render/aim agreement was accepted by the user with “works for me”.
- Final canonical balance changes are recorded in the catalogs and durable specifications.
- `just check` passed all role, Balance Lab, and retained-architecture checks.
- Final `just lint` passed formatting, 10 Balance Lab web tests/build, warning-denied Clippy for routing/client/server/Balance Lab, server feature isolation, sole-renderer validation, and map cleanup.
- Current client/server/Balance Lab suites passed 425/331/348 tests plus the routed Balance Lab handoff.
- All 90 routed network scenarios passed after mechanics fixtures were decoupled from superseded balance values.
- All 12 performance gates passed; current 32-shot Scatter load emitted 160 projectiles at 3.064167 ms p95.
- `git diff --check` passed.
- M03 and V12 are marked Complete, the learning review is recorded, docs/index and AGENTS.md are reconciled, and V13’s entry gate is satisfied.
- Unrelated user files remain untouched.
